import { createAction } from 'redux-actions';

export const initCategories = createAction('CATEGORIES_INIT');
export const selectCategory = createAction('CATEGORY_SELECT');

export default this;
